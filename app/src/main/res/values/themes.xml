<resources xmlns:tools="http://schemas.android.com/tools">
    <!-- Base application theme -->
    <style name="Base.Theme.MutasiBPS" parent="Theme.Material3.DayNight.NoActionBar">
        <!-- Light theme customizations -->
        <item name="colorPrimary">@color/biru</item>
        <item name="colorOnPrimary">@android:color/white</item> <!-- Text color on primary color -->
        <item name="colorSecondary">@color/biru</item> <!-- If you want to apply it elsewhere -->
        <item name="colorOnSecondary">@android:color/white</item> <!-- Text color on secondary -->
    </style>

    <style name="SpinnerTextStyle" parent="Widget.AppCompat.Spinner">
        <item name="android:textColor">@color/white</item> <!-- Warna teks dropdown -->
        <item name="android:textSize">16sp</item> <!-- Ukuran teks -->
    </style>

    <style name="Theme.MutasiBPS" parent="Base.Theme.MutasiBPS" />
</resources>
